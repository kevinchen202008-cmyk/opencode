### OpenCode 组织级可靠性与安全使用指南

> 适用对象：在团队或组织范围内部署和使用 OpenCode 作为 AI 辅助开发/自动化平台的技术负责人与工程团队。

---

### 一、目标与范围

- **目标**：在组织级场景下，可靠、安全地运行 OpenCode，避免将其当作「仅在本机试验」的工具，而是作为**可依赖的基础设施组件**。
- **范围**：
  - OpenCode Server 的部署与运行稳定性
  - 数据安全与持久化（会话、配置、日志、Git 仓库）
  - Agent/工具/MCP/Skill/插件的权限与越权防护
  - 升级与回滚策略
  - 可观测性与运维

---

### 二、部署与运行稳定性

#### 2.1 环境划分

- **推荐至少三层环境**：
  - `dev`：跟进上游较新的版本，用于内部试验和插件开发。
  - `staging`：贴近生产配置，做回归与兼容性验证。
  - `prod`：只运行经过 staging 验证的版本，服务真实团队与项目。
- 各环境应有**独立的配置、数据存储与密钥**，避免交叉污染。

#### 2.2 部署形态

- **建议容器化部署**：
  - 使用 Docker/Kubernetes/Nomad 等运行 OpenCode Server。
  - 在前面增加反向代理（nginx/traefik 等），统一做：
    - TLS 终止
    - 访问控制与限流
    - 健康检查与重试
- **单实例 vs 多实例**：
  - 小团队可以每个环境一个实例。
  - 大团队建议按「业务域/部门/安全级别」拆分多个实例，减少 blast radius。

#### 2.3 数据存储与备份

- 默认使用 SQLite 文件存储：
  - 适合作为 **单实例、单节点** 的持久化方案。
  - 必须配置**定期备份**（文件级快照），并在预生产环境验证**恢复流程**。
- 对于组织级高要求场景，建议：
  - 将 Drizzle ORM schema 迁移到集中式数据库（如 Postgres）。
  - 使用数据库级备份与 Point-in-time recovery（PITR）。

---

### 三、数据安全与访问控制

#### 3.1 会话与日志

- 会话（Session）和消息历史可能包含：
  - 源代码
  - 配置文件（包括敏感信息）
  - 业务逻辑与架构信息
- 建议：
  - 明确 Session/日志的 **保留周期** 与清理策略（如 30/90 天）。
  - 对存放 SQLite/日志的目录启用：
    - 文件系统权限控制（仅运维账号可读）
    - 如有可能，启用磁盘/卷加密。

#### 3.2 Git 仓库与工作目录

- OpenCode 深度集成 Git：
  - 自动检测 `.git` 确定项目/工作区。
  - 使用 `worktree` 创建沙箱分支。
- 组织级使用时：
  - 明确**哪些仓库允许 Agent 自动修改**，哪些只能只读：
    - 生产仓库：默认只允许只读 Agent（plan/explore），禁止自动写入。
    - 开发/测试仓库：可以启用 build Agent 与写工具，但配合分支策略（feature 分支、沙箱 worktree）。
  - 禁止将 `.git`、`.opencode` 等目录暴露到不可信机器或共享存储中。

---

### 四、权限体系与工具安全

#### 4.1 全局权限模板

- 利用 `opencode.json` 中的 `permission` 字段，为组织定义几套标准模板，例如：

```jsonc
{
  "permission": {
    "bash": {
      "*": "ask"
    },
    "mcp_*": {
      "*": "ask"
    },
    "write": {
      "*": "allow"
    },
    "edit": {
      "*": "allow"
    }
  }
}
```

- 按场景区分：
  - **生产仓库模板**：`bash`、高危 MCP 工具默认 `deny` 或 `ask`；只读 Agent 为主。
  - **开发仓库模板**：允许更多工具，但对基础设施相关 MCP 工具（K8s、云 API、数据库写）必须 `ask`。

#### 4.2 Agent 级权限覆盖

- 对于关键 Agent（如「自动部署 Agent」「CI 修复 Agent」），在 Agent frontmatter 或全局 config 中单独收紧权限：
  - 只允许特定工具（如 `read`/`write`/`apply_patch`），明确禁止 `bash` 或限制命令白名单。
  - 对 Skill 加模式控制，例如：

```yaml
---
permission:
  skill:
    "internal-*": "deny"
    "release-*": "allow"
---
```

#### 4.3 MCP 工具与外部系统

- MCP 提供强大但高风险的能力（数据库、监控、云资源等），建议：
  - 为 MCP 工具统一使用前缀（如 `db_*`、`k8s_*`、`cloud_*`），便于权限模式匹配。
  - 在组织级配置中：
    - 生产环境：默认 `deny`，只对特定 Agent/项目显式 `allow` 或 `ask`。
    - 开发环境：可以 `ask`，保留人工确认。
  - 针对「写生产数据库」「改生产集群」类 MCP 工具，要求**双因子批准流程**（例如 Agent 提议 → 人工 review + 执行）。

---

### 五、插件、Skills 与扩展管理

#### 5.1 插件（Plugins）

- 插件可以 Hook 多个关键点（如 `tool.execute.before/after`、`shell.env`、`chat.params` 等），组织级需要：
  - 建立**插件白名单**：只允许经过代码审查、安全评估的插件进入生产环境。
  - 禁止在项目仓库里随意引入未审计的第三方插件。
  - 对生产环境插件变更采用「PR + 审批 + 回滚预案」流程。

#### 5.2 Skills（SKILL.md）

- Skills 本质是文本说明和引用文件，不直接执行代码；但它们会驱动 Agent 的行为，因此：
  - 组织级维护一个集中 `skills` 仓库，通过 `skills.urls` 或集中配置分发。
  - 对敏感 skill（如 `git-release`、`prod-maintenance`）采用更严格的权限规则。
  - 不建议在未知来源的远程 URL 上加载 skill。

---

### 六、模型与 Provider 的可靠性与成本

#### 6.1 多 Provider 与 Failover

- 为重要 Agent 配置**主备模型**：
  - 主：高质量模型（如最新一代 Claude/GPT/Gemini）。
  - 备：成本更低或本地模型，在主提供商故障或配额用尽时自动降级。
- 对长对话或大上下文任务，使用专门配置的模型，避免通用模型被频繁打满上下文窗口。

#### 6.2 成本与配额控制

- 为每个 Provider 配置：
  - 单日/单月调用量限制。
  - 单个会话或单个 Agent 的最大 token 限制。
- 与内部计费/监控系统集成，对模型调用成本做可视化和预算管理。

---

### 七、升级、回滚与兼容性

#### 7.1 版本策略

- 不直接跟随上游 `dev` 分支，而是：
  - 选择稳定 tag 或 release 作为组织内部基线版本。
  - 在 dev/staging 环境充分回归后，再发布到 prod。
- 保留最近 1–2 个可用版本的镜像或安装包，以便出现问题时快速回滚。

#### 7.2 配置与 Schema 变更

- 在升级前：
  - 对比新旧版本的配置 schema（`opencode.json` 字段变化）。
  - 检查数据库迁移与回滚脚本。
- 在 staging 上执行一次完整的「迁移 → 回滚 → 再迁移」演练，验证无数据丢失或格式损坏。

---

### 八、可观测性与审计

#### 8.1 日志与监控

- 将 OpenCode 的 Server 日志输出到集中式日志系统（ELK、Loki、Datadog 等），至少包含：
  - HTTP 请求路径、状态码（可做错误率统计）。
  - 工具执行日志（成功/失败及关键参数摘要）。
  - Provider/MCP 调用错误与超时。
- 为关键指标提供监控：
  - QPS、P95/P99 延迟。
  - 工具调用分布（哪些工具最常被用）。
  - 模型调用量与错误率。

#### 8.2 健康检查与告警

- 在反向代理或编排系统中配置健康检查（如 GET `/doc` 或轻量级 `/healthz` 辅助端点）。
- 结合监控设置告警规则：
  - Server 不可达。
  - 错误率/超时率异常升高。
  - 模型或 MCP 调用持续失败。

#### 8.3 审计日志

- 对以下操作建议记录审计日志（含时间、Agent、用户、目标资源、参数摘要）：
  - 写文件（`write`/`edit`/`apply_patch`）。
  - 执行 Shell（`bash`）。
  - 调用高危 MCP 工具（如 DB 写操作、云控制 API）。
- 审计日志应写入只读/受控存储，便于安全与合规审查。

---

### 九、安全与合规注意事项

- **密钥管理**：
  - Provider/API Keys/MCP 凭证等推荐存放在组织级 Secret 管理系统（Vault/KMS 等），由 CI/CD 或配置服务注入环境变量，避免直接写入仓库或明文配置。
- **最小权限原则**：
  - 对运维账号、API Token、MCP 服务账号都执行最小权限策略。
  - 对「谁能修改 OpenCode 配置」「谁能部署新版本」进行角色划分与审批。
- **隐私与合规**：
  - 对会话中可能涉及的用户数据和业务数据，参照公司内部隐私与合规政策执行（如 GDPR、数据出境限制等）。
  - 明确哪些环境可以访问真实生产数据，哪些只能访问脱敏/仿真数据。

---

### 十、落地建议（Check-list）

1. **部署**：为 OpenCode 建立 dev/staging/prod 三层环境 + 反向代理 + 数据备份。
2. **权限**：在组织级配置统一的 `permission` 与 Agent 模板，特别收紧 `bash` 与高危 MCP 工具。
3. **代码范围**：区分「只读仓库」与「可写仓库」，并在配置与文档中明确标注。
4. **扩展**：建立集中管理的插件与 Skills 仓库，制定引入与变更流程。
5. **模型**：配置主备模型与调用配额，接入内部监控与成本分析。
6. **升级与回滚**：制定版本选型策略与回滚预案，在 staging 上做完整演练。
7. **可观测性**：打通日志与监控，增加健康检查与审计日志，形成日常运维 Dashboard。

将上述实践与组织已有的 DevOps/SRE/SecOps 流程结合，可以把 OpenCode 从「强大的本地工具」提升为「可靠可控的团队级 AI 开发平台」。

